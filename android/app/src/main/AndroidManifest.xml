<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- WiFi info for MITM/hostile-network detection (security telemetry).
         Reading SSID/BSSID requires location since Android 8.0. Requested at
         runtime ONLY after a TLS interception is detected, never at launch. -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Voice Call Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- File & Storage Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- Notification Permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Full-Screen Intent Permission (Android 10+) for urgent notifications -->
    <!-- Used for emergency-style alerts that must be seen immediately -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- APK Installation Permission -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- Wake Lock for Voice Calls -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Foreground Service for Background WebSocket & Notifications -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <!-- Fernwartung screen capture: Android 14+ requires an active mediaProjection
         foreground service before MediaProjection can start, and to keep the
         capture alive when the app is backgrounded. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- Fernwartung: waehrend einer Sitzung kann gesprochen werden. Ab
         Android 14 braucht ein Vordergrunddienst, der das Mikrofon benutzt,
         waehrend die App im Hintergrund ist, DIESE Berechtigung und den
         Diensttyp `microphone` — sonst wird der Ton stumm, sobald das Mitglied
         die App verlaesst, also genau dann, wenn ihm geholfen wird. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Battery Optimization Exemption (keeps WebSocket alive) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Receive Boot Completed (restart service on reboot) -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Exact Alarms for service restart (Android 12+) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:label="MitgliederPortal - ICD360S e.V"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Background Service for persistent WebSocket connection -->
        <!-- Security: exported=false to prevent external apps from starting service -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="remoteMessaging"
            android:stopWithTask="false"
            android:exported="false"
            tools:replace="android:exported" />

        <!-- Boot Receiver to restart service on device reboot -->
        <!-- Security: exported=false with permission to receive system broadcast -->
        <receiver
            android:name="id.flutter.flutter_background_service.BootReceiver"
            android:exported="false"
            android:enabled="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED"
            tools:replace="android:exported">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Custom Restart Receiver for service restart when app is closed -->
        <receiver
            android:name=".RestartReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="de.icd360s.mitglieder.RESTART_SERVICE" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Restart Service monitors app task removal (swipe away) -->
        <service
            android:name=".RestartService"
            android:enabled="true"
            android:exported="false"
            android:stopWithTask="false" />

        <!-- Fernwartung: mediaProjection foreground service. Kept running only for
             the duration of a consented remote-support session so MediaProjection
             is allowed to start (Android 14+) and survives backgrounding. -->
        <service
            android:name=".ScreenCaptureService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaProjection|microphone" />

        <!-- Fernwartung: Fernsteuerung. Android laesst eine normale App keine
             Eingaben in andere Apps schicken (INJECT_EVENTS ist signature-only);
             der einzige offene Weg ist ein AccessibilityService mit
             canPerformGestures. Genau so machen es TeamViewer QuickSupport und
             RustDesk.
             ⚠️ Die Rechte stehen in res/xml/fernwartung_accessibility.xml und
             sind bewusst minimal: KEIN canRetrieveWindowContent, also kein
             Auslesen des Bildschirms, und typeNone, also kein Mithoeren von
             Ereignissen. Dazu kommt ein zweites Schloss in Dart: ohne laufende,
             zugestimmte Sitzung wird jede Geste verworfen.
             ⚠️ exported MUSS true sein — das System bindet den Dienst. Geschuetzt
             ist er ueber die Signaturberechtigung BIND_ACCESSIBILITY_SERVICE,
             die nur das System selbst besitzt. -->
        <service
            android:name=".FernwartungService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/fernwartung_dienst_kurz"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/fernwartung_accessibility" />
        </service>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Required by url_launcher on Android 11+ (package-visibility
             restriction). Without these, canLaunchUrl returns false
             even when the user has a dialer / SMS app installed, and
             the "Sună-ne" / "Trimite SMS" buttons on Claudiu's
             emergency sheet silently no-op. See:
               https://pub.dev/packages/url_launcher#android -->
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="sms"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="sms"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.SENDTO"/>
            <data android:scheme="mailto"/>
        </intent>
    </queries>
</manifest>
